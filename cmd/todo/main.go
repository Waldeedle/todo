package main

func main() {
	// e := echo.New()
	// e.Debug = true

	// db, err := db.AddDB()
	// if err != nil {
	// 	panic(err)
	// }

	// todoRepository := todo.NewRepository(db)
	// todoService := todo.NewService(todoRepository)
	// puapi := public_api.New(todoService)

	// err = public_api.AddRoutes(e)
	// if err != nil {
	// 	panic(err)
	// }

	// err = private_api.AddRoutes(e)
	// if err != nil {
	// 	panic(err)
	// }

	// e.Logger.Fatal(e.Start(":3000"))
}
